// I just realized that the interface name
// sounds like Apple's IMessage. It's actually "I"
// as in the first letter of Interface.
interface IMessage {
  message: string;
  from: string;
  to: string;
}

export default IMessage;
